syntax = "proto3";

package tzero.v1.common;

import "buf/validate/validate.proto";

message PaymentReceipt {
  oneof details {
    option (buf.validate.oneof).required = true;
    SepaReceipt sepa = 10;
    SwiftReceipt swift = 30;
    StablecoinReceipt stablecoin = 40;
  };
}

message SepaReceipt {
}

message SwiftReceipt {

}

message StablecoinReceipt {
  string transaction_hash = 10 [(buf.validate.field).string = {
    min_len: 64,
    max_len: 66,
    pattern: "^(0x)?[a-fA-F0-9]{64}$"
  }]; // on-chain hex encoded transaction hash
}